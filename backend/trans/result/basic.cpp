// Автоматически сгенерированный код из Python
// Не требует внешних заголовочных файлов

int a = 15;
int b = 3;
int sum = (a + b);
int difference = (a - b);
int product = (a * b);
int quotient = (a / b);
// print("Результаты:")
// print(sum)
// print(difference)
// print(product)
// print(quotient)
int main() {
    // Автоматически сгенерированная главная функция
    return 0;
}